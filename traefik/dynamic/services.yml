# Traefik Dynamic Service Configuration
# This file defines backend services

http:
  services:
    # Frontend Service
    frontend:
      loadBalancer:
        servers:
          - url: "http://frontend:3000"

    # Backend API Service
    backend:
      loadBalancer:
        servers:
          - url: "http://backend:8080"

